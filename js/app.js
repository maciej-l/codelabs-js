(($) => {
    const appControler = (() => {
        return {
            init: () => {
                console.log('App is running');
            }
        }
    })();

    appControler.init();
})(jQuery);